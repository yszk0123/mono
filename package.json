{
  "private": true,
  "name": "mono",
  "version": "0.1.0",
  "main": "index.js",
  "repository": "https://github.com/yszk0123/mono",
  "author": "Yuji Suzuki <y0suzuki0123@gmail.com>",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write ./{client,server}/{src,database}/**/*.{js,css,ts,tsx}",
    "schema": "(cd server && yarn prepare) && (cd client && yarn schema)"
  },
  "devDependencies": {
    "prettier": "^1.10.2"
  }
}
